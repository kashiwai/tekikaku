# MPM prefork設定（LoadModuleは絶対に含めない - 二重ロード防止）
# モジュールのロードはmods-enabled/経由のみ

<IfModule mpm_prefork_module>
    StartServers             5
    MinSpareServers          5
    MaxSpareServers         10
    MaxRequestWorkers      150
    MaxConnectionsPerChild   0
</IfModule>